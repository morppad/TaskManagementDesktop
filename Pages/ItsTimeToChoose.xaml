<Page x:Class="TaskManagment.Pages.ItsTimeToChoose"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ItsTimeToChoose">

    <Grid>
        <!-- Центрируем содержимое -->
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Кнопка управления пользователями -->
            <Button Content="Управление пользователями"
                    Width="250"
                    Height="50"
                    FontSize="16"
                    Click="ManageUserButton_Click"/>

            <!-- Кнопка управления задачами -->
            <Button Content="Управление задачами"
                    Width="250"
                    Height="50"
                    FontSize="16"
                   
                    Click="ManageTaskButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
